# Ray-Tracing-IFC-Viewer

This project provides a minimal starting point for a Vulkan-based viewer.

## Building

The project uses CMake. Make sure the Vulkan SDK is installed on your system.

```bash
cmake -B build
cmake --build build
```

The resulting executable `raytracing_ifc_viewer` will be created in the `build` directory.
